<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_food_burger"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_food_burger"
        android:supportsRtl="true"
        android:theme="@style/Theme.FoodMenu"
        tools:targetApi="31">

        <!-- Main screen: shows the list of food cards -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Detail screen: shows full info for one food item -->
        <activity
            android:name=".FoodDetailActivity"
            android:parentActivityName=".MainActivity" />

    </application>
</manifest>
